apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributed-compute-operator.fullname" . }}
  labels:
    {{- include "distributed-compute-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distributed-compute-operator.selectorLabels" . | nindent 4 }}
